import { Box, Typography } from '@mui/material'

const Dashboard = () => {
    return (
        <Box sx={{
            width: '100%',
        }}>
            <Typography>Dashboard</Typography>
            <Typography>Welcome SAM</Typography>
        </Box>
    )
}

export default Dashboard